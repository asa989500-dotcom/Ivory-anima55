# المرحلة ٩٣ — العطل كان مني، والفاحص الذي كان يجب أن يمسكه لم يكن موجوداً

## ما حدث بالضبط

الأخطاء الخمسة كلّها صدى سطرٍ واحد:

```
res://scripts/ui/bspline_room.gd:1413  Expected statement, found "Indent" instead
res://scripts/ui/bspline_room.gd:1427  Unindent doesn't match the previous indentation level
```

في المرحلة ٩٢ أضفتُ `@warning_ignore("integer_division")` فوق ثلاثة أسطر قسمةٍ
صحيحة. **وكانت الثلاثة تحمل التأشيرة أصلاً.** لم أنظر. والأسوأ أنّ نصّ البحث
الذي استعملته كان `\tvar ay: int = at / cols` بجدولةٍ واحدة، بينما السطر
الحقيقي بثلاث — فطابق ذيل السطر، وأنتج هذا:

```
			@warning_ignore("integer_division")
			@warning_ignore("integer_division")
	var ay: int = at / cols          ← جدولة واحدة داخل كتلةٍ بثلاث
```

تأشيرة مكرّرة، وتصريحٌ خرج من كتلته. فسقط `bspline_room.gd`، ثمّ سقط `main.gd`
معه لأنّه يذكر `BSplineRoom` — **خمسة أخطاء من إدخالٍ واحدٍ أخرق**.

أُعيدت المواضع الثلاثة (اثنان في `bspline_room.gd`، وواحد في `puppet_warp.gd`)
إلى ما كانت عليه بالضبط قبل أن ألمسها.

## لكن هذا ليس العطل الحقيقي

**العطل الحقيقي أنّ ستّة فواحص قرأت ملفاً لا يستطيع Godot تحليله وقالت إنّه
نظيف.**

وسببه واضح بأثر رجعي: الفواحص الستّة تقرأ الأسماء، والعودات، والأسطر المكسورة،
والنطاقات، والمراجع بين الملفّات، وأطوال الملفّات. ولا واحد منها ينظر إلى **شكل**
الكود. وفي لغةٍ تكون فيها المسافة البادئة هي بنية الكتل نفسها، كانت تلك ثغرةً
بحجم اللغة.

## `tools/check_indent.py`

يقرأ الملفّ **جُملاً لا أسطراً** — فالجملة قد تمتدّ على عدّة أسطر، والحقيقتان
المطلوبتان عنها في طرفيها المتقابلين: المسافة البادئة على سطرها **الأول**،
والنقطتان اللتان تفتحان الكتلة على سطرها **الأخير**. (المحاولة الأولى قرأت
سطراً سطراً فاشتكت ٢٥٣ شكوى، كلّها خطؤها هي.)

ثمّ يتحقّق من ثلاثة:

- المسافة البادئة بجداول لا فراغات
- إن فتح ما قبلها كتلةً، فهي أعمق بمستوىً **واحد** بالضبط
- وإلّا فهي على مستوىً مفتوحٍ فعلاً: مستوى سابقتها، أو رجوعٌ إلى مستوىً أسّسه
  الملفّ من قبل

وهذا هو كلّ قانون الكتل في GDScript، ويكفي لمسك كلّ ما يُنتج
`Expected statement, found "Indent"` و`Unindent doesn't match`.

**وأُثبت** بإعادة العطل نفسه عمداً:

```
scripts/ui/bspline_room.gd:1412  indented deeper with no block opened above it
scripts/ui/bspline_room.gd:1426  unindent does not match any open level [0, 1, 3, 4, 5]
```

نفس الموضعين اللذين أبلغ عنهما Godot.

ويعمل **أولاً** في `check_all.py`، لأنّه الوحيد الذي يفحص بنية الملفّ: ملفٌّ
مكسور البنية لا يُحلَّل أصلاً، وكلّ شكوى أخرى منه — ومن كلّ ملفٍّ يذكر صنفه —
ضجيجٌ يقف أمام العطل الحقيقي الوحيد.

## وكنسٌ للمشروع كلّه

بما أنّ الأداة صارت موجودة، مُرِّر المشروع كلّه عليها بحثاً عن نفس صنف الضرر —
إدخالٌ فاشل لا تمسكه العين — وعن التأشيرات المكرّرة. **صفر في الاثنين.**

## الحساب

| | المرحلة ٩٠ | الآن |
|---|---|---|
| فواحص ساكنة | ٤ | **٧** |
| ما تمسكه | أسماء، عودات، أسطر مكسورة، نطاقات | + بنية المسافة البادئة، + المراجع بين الملفّات، + أطوال الملفّات |

وكلّ واحدٍ من الثلاثة الجديدة أُضيف بعد أن أفلت منه عطلٌ وصل إليك. هذا ليس
اعتذاراً — هو الطريقة الوحيدة التي تُبنى بها هذه الأدوات: عطلٌ يمرّ، ثمّ فاحصٌ
يمنع صنفه كلّه من المرور مرّة أخرى.

```
python3 tools/check_all.py .        # ٧ من ٧
godot --headless --path . --script tests/run_tests.gd
```
