# إصلاح استجابة الفرشاة وحذف أداة تشويه اللون

## النتيجة

أُصلح مسار الرسم بحيث لا تتوقف الفرشاة عندما تحتوي الطبقة على عظام. ما زال لمس المفصل يذهب إلى أداة تحريك العظام أولًا، بينما أي لمس آخر على الطبقة نفسها يتابع إلى أداة الفرشاة أو الأداة المختارة. أُضيف قيد يمنع نقاط الفرشاة من إنشاء حبر خارج حدود صفحة المشروع.

أُزيل خيار **Smudge / تلطيخ** من لوحة الدمج الموجودة أسفل الفرشاة، بما في ذلك صف الزر الخاص به. لم تُحذف أدوات المزج الأخرى أو مسار الرسم العادي.

## حواجز C++ الثلاثة عشر

تتحقق `BrushSafety13` من: القيم finite، النقاط finite، حجم الختم، رقم النسخة، الشفافية، الضغط، المسافة بين الختمات، نصف القطر، حدود الصفحة، عدد نقاط المسار، الترتيب الزمني، منع إعادة الدخول، وقابلية الطبقة للرسم. كما رُبط حارس حجم الطلب مباشرة بدالة `IvoryBrush::stamp`.

## الملفات المعدلة

| المسار | التغيير |
|---|---|
| `scripts/canvas_view.gd` | استمرار الرسم على الطبقات ذات العظام، وتقييد نقاط الفرشاة داخل الصفحة |
| `scripts/ui/tool_panels.gd` | حذف خيار Smudge / تلطيخ من الواجهة |
| `native/ivory/src/ivory_brush.cpp` | تطبيق فحص حجم طلب الختم قبل التوليد |
| `native/ivory/src/ivory_brush_safety.h` | حواجز C++ الثلاثة عشر |
| `native/ivory/tests/test_brush_safety.cpp` | خمس مجموعات اختبار، 29 تحققًا |
| `tests/test_brush_regression.gd` | اختبار مصدر لمسار الفرشاة وحذف خيار التلطيخ |

## التحقق

- اختبار الفرشاة الأصلي: **124 تحققًا، 0 فشل**.
- اختبار الحماية الجديد: **29 تحققًا، 0 فشل**، موزعة على خمس مجموعات.
- مجموع اختبارات الفرشاة: **153 تحققًا، 0 فشل**.
- فحص توجيه الإدخال: **نجح بلا فشل**.
- فحص التحذيرات: **0 تحذيرات جديدة**.

تعذر تشغيل Godot headless لأن نسخة Godot غير موجودة في بيئة التسليم. يوجد فشل مستقل سابق في فحص حجم المشروع بسبب `scripts/puppet_warp.gd` الذي يتجاوز الحد المسجل بثلاثة أسطر؛ لم تُضف هذه الزيادة في هذا الإصلاح.
