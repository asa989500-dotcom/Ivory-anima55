# Stage 168 — Touch and Bone Stability

## المطلوب

تحسين استقبال اللمس في الرسم والصور والطبقات العادية، ومنع الرجفة عند سحب أي نقطة من نقاط العظام، مع إبقاء الصورة المستوردة جزءًا من محتوى الطبقة نفسه لا مسارًا خاصًا منفصلًا.

## ما تغير في C++

تم توسيع `IvoryTouch` في:

```text
native/ivory/src/ivory_touch.h
native/ivory/src/ivory_touch.cpp
```

الإضافة الأساسية هي `deadband_px`، وهي منطقة صمت بوحدة بكسل الشاشة. عندما تتحرك نقطة اللمس داخل هذه المنطقة لا يتم إدخال الحركة إلى مشتق السرعة، لأن إدخالها سيجعل المرشح يعتقد أن الإصبع تحرك فعلًا ويفتح التنعيم بدل تهدئته.

التسلسل الجديد:

```text
عينة الشاشة
    ↓
حد الرجفة deadband
    ↓
تقدير سرعة مفلتر
    ↓
One Euro adaptive filter
    ↓
lead اختياري لعناصر التحكم فقط
    ↓
النقطة النهائية
```

واجهة `configure` القديمة ما زالت صالحة؛ قيمة `deadband_px` اختيارية وتبدأ من الصفر. لذلك لا تنكسر الاستدعاءات القديمة.

## إعدادات الاستخدام

### العظام

يستخدم `BoneHandle` حدًا أكبر قليلًا:

```text
MOTION_EPS_PX = 0.8
```

وهو مناسب لنقاط التحكم البنيوية؛ يمنع رجفة الإصبع دون منع حركة مقصودة واضحة.

### الرسم

يستخدم `StrokeSmoother` حدًا أصغر:

```text
TOUCH_DEADBAND_PX = 0.25
```

الرسم لا يستخدم lead؛ لأن توقع موضع الفرشاة سيضيف حبرًا لم يمر به الإصبع. المرشح هنا ينظف الضجيج فقط ويحافظ على مكان اليد الحقيقي.

## الصور والطبقات العادية

الصورة المستوردة في IVORY تدخل مسار التحديد ثم تُثبت على `PaintSurface` عند التأكيد. بعد ذلك تصبح بياناتها جزءًا من محتوى الطبقة، مثل أي رسم مرسوم بالفرشاة. `RigSkin.bind_layer()` يقرأ `content_bounds()` و`read_region()` من الطبقة نفسها، لذلك لا يوجد مسار عظام خاص بالصورة الخارجية.

بمعنى عملي:

```text
رسم بالفرشاة ─┐
              ├─ PaintSurface ─ RigSkin ─ BoneRig
صورة مستوردة ─┘
```

لا يتم تعديل البكسلات أثناء التحريك؛ يتم إنشاء جلد Mesh مؤقت يعرض محتوى الطبقة، ثم تعود الطبقة الأصلية عند إزالة الجلد. هذا يحافظ على الجودة ويمنع إعادة أخذ عينات الصورة في كل حركة.

## لماذا لم نضع deadband على العظام داخل solver نفسه؟

لأن solver العظام يجب أن يبقى رياضيًا وحتميًا، ولا ينبغي أن يعرف إن كانت الحركة جاءت من إصبع أو من Timeline أو من ملف. حد الرجفة موضوع عند طبقة الإدخال قبل solver، لذلك:

- السحب اليدوي يستفيد منه.
- التشغيل من الخط الزمني لا يتأثر.
- IK البرمجي لا يصبح عالقًا بسبب عتبة لمس.
- نفس solver يبقى صالحًا للفأرة والقلم واللمس.

## الاختبارات

أضيف اختبار مصدر مستقل:

```bash
python3 tools/test_touch_bone_stability.py
```

ويتحقق من:

- وجود deadband في واجهة C++.
- تطبيقه قبل حساب السرعة.
- بقاء `configure` القديم صالحًا.
- إعداد العظام بقيمة 0.8 بكسل.
- إعداد الرسم بقيمة 0.25 بكسل.
- بقاء الصورة والرسم على مسار `PaintSurface` نفسه.

## ملاحظة بناء

المشروع المرفق لا يحتوي `native/ivory/godot-cpp`، ولذلك يمكن تشغيل اختبارات المصدر، لكن بناء GDExtension الكامل يحتاج جلب نسخة godot-cpp المطابقة لـ Godot 4.4 ثم تشغيل SCons. عدم وجود المكتبة لا يعطل IVORY؛ مسار GDScript الاحتياطي يبقى فعالًا.

## النتيجة

هذا التحديث لا يغير بنية الطبقات ولا صيغة المشروع ولا طريقة حفظ العظام. هو يحسن نقطة استقبال الإدخال فقط، ويمنع الرجفة قبل أن تصل إلى العظام أو الرسم، مع إبقاء الصور المستوردة رسومات عادية قابلة للربط بالعظام.
