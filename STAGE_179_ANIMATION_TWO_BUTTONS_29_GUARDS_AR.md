# Stage 179 — إصلاح جذري لزري اختيار غرفة الرسوم المتحركة

## نطاق التعديل

هذا الإصدار يركز على **شاشة اختيار نوع غرفة الرسوم المتحركة فقط**:

1. **Ivory**
2. **Stretchy Studio**

لم يتم تعديل منطق العظام أو Puppet Warp أو الرسم أو التايم لاين أو التصدير أو غرف أخرى.

## لماذا كان الحل السابق غير كافٍ؟

المشكلة لم تكن مجرد حجم الزرين. الـ picker السابق كان يعتمد على حاويات ترتيب (`MarginContainer`/`VBoxContainer`) بينما شاشة الإعدادات نفسها كانت تملك نظام تخطيط/تمرير مستقل. والأسوأ أن الـ C++ extension descriptor كان موجودًا باسم `.gdextension.disabled`، لذلك في نسخة لا تحتوي على مكتبة C++ مبنية كانت `ClassDB.instantiate("IvoryAnimationWorkspacePicker")` ترجع `null`، وبالتالي لا توجد أزرار أصلية أصلًا.

Stage 179 يعالج المسارين: **هندسة الزرين داخل C++** و **تحميل الامتداد عند وجود المكتبة المبنية**.

## الحلول الـ29 المطبقة كعقد أمان مستقلة

1. جعل الـ picker `PanelContainer` أصليًا من C++.
2. تثبيت عرض أساسي 430px.
3. تثبيت ارتفاع أساسي 236px.
4. استخدام `Control` واحد كسطح داخلي بدل قائمة ديناميكية.
5. إزالة الاعتماد على VBox لتحديد موضع الزرين.
6. إزالة الاعتماد على Grid لتحديد موضع الزرين.
7. عدم استخدام أي آلية تمرير داخل الـ picker.
8. تعطيل clipping للوحة.
9. إيقاف استقبال الإدخال على اللوحة نفسها لمنع تسرب الضغط.
10. رفع Z للوحة إلى طبقة عالية وثابتة.
11. جعل Z مستقلًا عن الأب.
12. رفع Z للسطح الداخلي.
13. إنشاء زر Ivory حقيقي بـ `memnew(Button)`.
14. إنشاء زر Stretchy Studio حقيقي بـ `memnew(Button)`.
15. تثبيت ارتفاع كل زر عند 78px.
16. تثبيت الحافتين الجانبيتين 20px.
17. تثبيت الفاصل العمودي 12px.
18. إجبار الزرين على `visible=true` عند البناء.
19. منع تعطيل الزرين.
20. منع وضع toggle mode على الزرين.
21. إيقاف input filter للزر الأول على `STOP`.
22. إيقاف input filter للزر الثاني على `STOP`.
23. ربط Ivory مباشرة بـ callback C++ الخاص به.
24. ربط Stretchy مباشرة بـ callback C++ الخاص به.
25. فحص أن كل زر ابن مباشر للسطح الداخلي الصحيح.
26. فحص أن عرض كل زر أكبر من صفر وأن ارتفاعه كامل.
27. فحص أن ترتيب الزرين عمودي ولا يوجد تداخل.
28. فحص أن الزر الثاني لا يخرج من حدود اللوحة.
29. فحص اتصال signal `pressed` لكلا الزرين قبل السماح لهما بالعمل.

## حماية إضافية أثناء التشغيل

`layout_fixed()` يعاد تشغيلها عند `READY` وعند `RESIZED`. وإذا حاول الأب إعطاء اللوحة حجمًا أكبر، يعاد حساب موضع الزرين من حجم اللوحة نفسها؛ لا يوجد scroll offset يمكن أن يدفع الزرين إلى أسفل قائمة.

`guard_passed_` لا يسمح بتنفيذ الاختيار إذا فشل عقد الأمان الـ29.

## الامتداد الأصلي

تم تفعيل `ivory.gdextension` من النسخة المعطلة السابقة. هذا لا يصنع مكتبة Android من العدم، لكنه يمنع خطأ مهمًا كان يجعل ملفات C++ موجودة في المشروع بينما Godot لا يحاول تحميلها.

يجب أن تكون مكتبة `libivory` المبنية للـ ABI المطلوب موجودة في `bin/` عند بناء/تشغيل النسخة الأصلية. ملف `SConstruct` الموجود في المشروع يحدد مسارات Android المطلوبة.

## الفحوص

- فاحص Stage 177 الحالي: **PASS**.
- فاحص Stage 179: **29/29 invariants PASS**.
- اختبارات هندسة regression: **20/20 PASS**.
- ضغط هندسي متكرر: **100/100 PASS**.
- Python syntax checks: **PASS**.

## حد الاختبار البيئي

لم يتم ادعاء نجاح تشغيل Godot نفسه: بيئة الفحص الحالية لا تحتوي على Godot executable ولا checkout حقيقي لـ `godot-cpp`، لذلك لا يمكن بناء `libivory` Android داخل هذه البيئة. الاختبارات التي تم تنفيذها هنا هي اختبارات مصدر، تكامل، هندسة وحماية؛ أما اختبار Android الفعلي فيتطلب بناء المكتبة بالـ NDK/Godot-cpp ثم تشغيل APK.
