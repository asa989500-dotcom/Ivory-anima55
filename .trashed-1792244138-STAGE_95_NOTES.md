# المرحلة ٩٥ — «المشاكل تتوارث»، وأنت محقّ في الكلمة

## الأخطاء الثلاثة

```
ai_bridge.gd:97   Cannot assign a value of type Callable to variable "index" with specified type int
ai_bridge.gd:107  Value of type "Callable" cannot be assigned to a variable of type "int"
ai_bridge.gd:145  Invalid argument for "_layer_sketch()": argument 1 should be "int" but is "Callable"
```

الثلاثة سببها واحد: `stack.active`.

`LayerStack` فيه **دالّة** اسمها `active()` تُرجع الطبقة، و**متغيّر** اسمه
`active_index` يحمل الرقم. وأنا كتبتُ `active` بلا قوسين، فصارت **مرجعاً
للدالّة نفسها** (`Callable`) لا رقماً. صار `active_index` في المواضع الثلاثة.

## ولماذا لم تمسكه ثمانية فواحص

هنا بيت القصيد. `check_references` **قرأ السطر ووجده سليماً**، لأنّ `LayerStack`
فيه فعلاً عضوٌ اسمه `active`. الاسم موجود؛ الخطأ في **نوع** ما هو، لا في
وجوده. وكلّ فواحصي كانت تسأل «هل هذا الاسم موجود؟» ولا واحد منها يسأل «وما
هو؟».

## ثلاث قدرات جديدة في `check_references.py`

**١ · يعرف الآن ما هو كلّ عضو** — دالّة أم متغيّراً وما نوعه المصرَّح — لا
مجرّد أنّه موجود.

**٢ · يتتبّع النوع عبر السلسلة.** `p.stack` حيث `p` من نوع
`ProjectManager.Project` و`Project.stack` مصرَّح `LayerStack` → يصل إلى
`LayerStack`. ونصف بيانات هذا المشروع تعيش في أصنافٍ داخلية
(`ProjectManager.Project`، `LayerStack.Layer`، `Character360.Pose`،
`Anim.Clip`)، ومحلّلٌ يتوقّف عند النقطة لا يستطيع تتبّع شيء. وفي هذه السلسلة
بالذات كان مختبئاً ثالث الأخطاء الثلاثة.

**٣ · يمسك الدالّة المستعملة كقيمة** في شكلين:

```
var x: int = a.b.method          ← إسناد إلى متغيّر بنوع مصرَّح
f(a.b.method)                    ← تمرير كوسيط
```

ويترك `connect` و`sort_custom` وأخواتها وشأنها — فتمرير الدالّة هناك هو
المقصود بالضبط.

**وأُثبتت الثلاثة** بإعادة كلّ خطأ عمداً؛ كلٌّ منها أعطى سطره ورقمه.

## ورابعة: عدد الوسائط

وأضفتُ ما كان ينقص أيضاً: فحص **عدد الوسائط** في كلّ نداءٍ لدالّةٍ في المشروع.
هذا أكثر ما تنتجه عمليّة الرفع: غيّرتُ أربع توقيعات في جلسةٍ واحدة هذا الأسبوع
(`fill_mask`، `premultiply`، `ease_lead`، `look_words`) ولكلٍّ منها مُستدعون.
Godot يمسك هذا — لكن **بعد** أن يُحلَّل الملفّ، وملفٌّ لا يُحلَّل يخفي كلّ واحدةٍ
منها خلف شكوى لا علاقة لها.

## الستّة عشر تحذيراً

`_sel_center`، `_grab_start`، `_placing`، `_filler`، `_unit_plate`،
وإشارتان… **اثنتان وعشرون، وكلّها إنذارٌ كاذب.** كلّ واحدة منها تُقرأ فعلاً —
`_sel_center` ثلاث عشرة مرّة — لكن من `canvas_marks.gd` بعد الرفع، وGodot لا
يرى عبر `p._sel_center` رجوعاً إلى تصريحها.

أُسكِتت **واحدةً واحدةً عند تصريحها**، لا للملفّ كلّه.

**ولأنّ إسكاتها يُفقد التحذير معناه، أُعيد السؤال بشكلٍ صحيح:**
`check_scope.py` صار يسأل «هل يقرأ هذا العضوَ أحدٌ **في أيّ ملفّ**؟» — فعضوٌ
تقرأه وحدةٌ مرفوعة سليم، وعضوٌ لا يقرأه أحد **كودٌ ميت ويُبلَّغ عنه**. النتيجة
اليوم: صفر ميت.

## والتدقيق الذي طلبته

مُرِّرت كلّ الملفّات التي كتبتُها أو رفعتُها منذ المرحلة ٩١ عبر المحلّل الجديد،
عضواً عضواً:

```
accesses into a project class: 1229 resolved, 0 unresolved
accesses into a built-in type: 159 (the engine's business, not judged)
```

ألفٌ ومئتان وتسعة وعشرون وصولاً إلى عضوٍ في صنفٍ من أصناف المشروع، كلّها
تُحلّ. والمئة والتسعة والخمسون الباقية أعضاءُ أنواعٍ مدمجة —
`Array.append`، `Color.r`، `Control.draw_rect` — ولا يستطيع فاحصٌ يعمل بلا
Godot الحكم عليها، وأقول ذلك بدل أن أدّعي تغطيةً ليست عندي.

## الحساب

| | ٩٠ | ٩١ | ٩٢ | ٩٣ | ٩٤ | ٩٥ |
|---|---|---|---|---|---|---|
| فواحص | ٤ | ٦ | ٦ | ٧ | ٨ | ٨ |
| ما تفحصه | الأسماء | + المراجع، الأحجام | | + المسافة البادئة | + النطاق، المعاملات | **+ الأنواع، السلاسل، عدد الوسائط، الكود الميت** |

الفواحص لم تزد عدداً هذه المرّة — **زادت عمقاً**. لأنّ الثغرة لم تكن في فحصٍ
غائب، بل في سؤالٍ كانت كلّها تطرحه ناقصاً: «هل الاسم موجود؟» بدل «وما هو؟».

```
python3 tools/check_all.py .        # ٨ من ٨
godot --headless --path . --script tests/run_tests.gd
```
