# Stage 91 — the AI unit

## أين تكتب مفتاحك — الجواب أولاً

**لا يوجد مشهد.** المشروع كلّه يُبنى من الكود؛ `scenes/main.tscn` ثمانية أسطر
لا تحمل إلا سطر السكربت. المفتاح مكانه سطرٌ واحد:

```
الملف:  scripts/ai_assistant.gd
السطر:  33
        const API_TOKEN: String = "Bearer YOUR_API_KEY_HERE"
```

محاطٌ بسطري `▼▼▼` و`▲▲▲` حتى لا تخطئه العين. أبقِ كلمة `Bearer` والمسافة
بعدها وبدّل ما بعدها فقط.

**والطريق الثاني، وهو الأصحّ:** `الإعدادات ◂ الذكاء الاصطناعي` — حقلٌ مخفيّ
يحفظ المفتاح في `user://ai_key.txt`، **ويتقدّم على الثابت**. لماذا هو الأصحّ:
ثابتٌ مُجمَّع داخل APK يستخرجه أيّ شخص يملك الملف في دقيقة، ومفتاحٌ مسرَّب
يُنفَق على حسابك أنت. الثابت موجود لأنّك طلبته ولأنّه أسرع طريق للتجربة.

---

## البنية — كما حدّدتها

```
AIAssistant  (Node، يُصنع مرّة في main)
  └─ NetworkRequest  (HTTPRequest)
```

`ENDPOINT` إلى FLUX.1-schnell · `HTTPClient.METHOD_POST` · ترويسة
`Authorization` و`Content-Type: application/json` · الصور عبر
`save_png_to_buffer()` ثمّ `Marshalls.raw_to_base64()` كـ Data URI ·
`strength` داخل `parameters` · الرجوع عبر `load_png_from_buffer()` ثمّ
`ImageTexture.create_from_image()` · و**503** تُعامَل كنموذجٍ يستيقظ لا كعطل:
ستّ محاولات بانتظارٍ متزايد، ومعها ترويسة `x-wait-for-model`.

**طابور واحد لثلاث ميزات.** المحسّن والغرفة ومالئ الدوران ينتظرون في صفٍّ
واحد. نسختان من كود HTTP هما المكان الذي يعيش فيه العطل الأوّل بعد إصلاحه.

**وشيء لم تذكره وكان لازماً:** بكسلات IVORY مضروبةٌ سلفاً بالشفافية. تُرسَل
كما هي فتصل كلّ حافة ناعمة أغمق ممّا رُسمت، ويعيد النموذج إنتاج التعتيم
بأمانة. فالصورة تُفكّ قبل الإرسال وتُضرَب بعد الاستقبال، والاختبار يتحقّق أنّ
الرحلة ذهاباً وإياباً لا تُزيح بكسلاً أكثر من خطوة تقريب — وإلّا لاسودّت
الحواف بعد اثني عشر تحسيناً.

---

## الميزات الثلاث

### ١ · تحسين الطبقة

`إعدادات الطبقة ◂ تحسين`. يقتطع المرسوم وحده (`content_bounds`) لا الفراغ
حوله — فصورةٌ في زاوية ورقة بيضاء تعود صورةً في زاوية ورقة بيضاء — ويرسله
بـ `strength = 0.38`، منخفضة عمداً: تنظيف الخطوط وتصحيح النسب والتوجّه، لا
رسمةً أخرى.

والناتج يهبط على **طبقة التحسين**: طبقة عادية تماماً، بالاسم فقط للتمييز،
والأصل تحتها سليم يُقارَن بضغط العين. وتصل عائمة لا مثبّتة — فهي اقتراح حتى
تضعها أنت.

### ٢ · AI-powered image design

في البوصلة، بأيقونتك (`assets/icons/ai_design.png`).

**محادثة لا نموذج إدخال.** أحدٌ لا يصف صورة صحيحةً من أوّل مرّة؛ ما يحدث فعلاً
هو «فارس» ← عادي جداً ← «فارس بدرع فضي» ← الزاوية خطأ ← «…من الأسفل». وكلّ
واحدة تعديلٌ صغير لسابقتها، ونموذجٌ يرمي الكلمات بين المحاولتين يجعلك تعيد
كتابة الجملة في كلّ مرّة. هنا الخيط يبقى: كلّ محاولة على الشاشة مع صورتها.

المساران كلاهما: **نصّ وحده**، أو **نصّ مع رسم الطبقة** بمربع اختيار، ومزلاج
لمقدار ابتعاده عن رسمك. والأربعة التي طلبتها: استيراد صورة · حفظها في ذاكرة
المحادثة · إدخالها في المشروع · حفظها في الجهاز.

### ٣ · Character 360 — ملء الفراغات

ثماني رسمات = خمس وأربعون درجة لكلّ خطوة، وهي مسافة طويلة لكتفٍ أن يقطعها بين
صورتين. `TurnMorph` يشوّه إحداهما على الأخرى ويحسن ذلك، لكنّ التشويه تخمينٌ عن
المنتصف ويسوء كلّما اتّسعت الفجوة: عند ٤٥° لا بدّ من اختراع ذراعٍ تعبر الجسد،
وعند ٢٢٫٥° يكفي تحريكها.

فعند ورود ثماني صور، تُطلَب ثمانٍ أخرى عند الزوايا الوسطى، تلقائياً وقت
الاستيراد. و`Character360.between` يقسم الدائرة على عدد الوضعيات — فكلّ ما
تحتها يصير أنعم بالضعف دون أن يُخبَر، **والعظام تقطع نصف المسافة بين وضعية
وأخرى**.

الإجابات تعود عبر الشبكة، أي **بأيّ ترتيب**، وكلٌّ منها تجد مكانها الصحيح
بالعدّ لا بالافتراض — واختبارٌ يتحقّق من ذلك بإدخال الثانية قبل الأولى.
والمولَّدة موسومة (`from_ai`)، وتُحذف كلّها بأمرٍ واحد دون المساس بالمستورد.

### علامة التحميل

قوسٌ يدور أعلى الشاشة، مع اسم ما يجري وعدد المنتظرين وزرّ إلغاء. **لا تبتلع
لمسة موجّهة للرسم** — الرسم أثناء صنع صورة شيءٌ معقول تماماً، وأخذ التطبيق
لمدّة الانتظار يجعل الإجابة البطيئة تبدو تعلّقاً.

سببها: ثماني ثوانٍ بلا تغيّرٍ على الشاشة تجعل المستخدم يضغط ثانية، فيصير
طلبان، كلاهما يكلّف، والثاني يمحو الأوّل.

---

## إذن الشبكة

`permissions/internet` كان **`false`** في المرحلة ٩٠. بدونه لا يفشل APK
بأدب — لا يتّصل أبداً، وهو ما يُقرأ كـ«الذكاء الاصطناعي معطّل». صار `true`،
ومعه `ACCESS_NETWORK_STATE` ليقول «لا شبكة» بدل أن ينتظر دقيقتين.

---

## وعطلٌ في أدواتي أنا

أداة `lift_module.py` من المرحلة ٩٠ كانت **تعيد الكتابة داخل النصوص
المقتبسة**. عند نقل كود الذكاء الاصطناعي خارج `main.gd` صار:

```gdscript
open_panel = "p.ai"                        # كان "ai"
call_deferred("p._fit_panel_to_content")   # دالّة لا وجود لها
```

**كلاهما يُترجَم بلا خطأ ويفشل على التابلت** — نفس صنف العطل الذي بُنيت الأدوات
كلها لمنعه. أُصلحت الأداة (تُنحّي النصوص قبل أن تعيد كتابة أي شيء)، وفُحصت
النقلتان السابقتان: نظيفتان.

**وحارس الحجم أطلق إنذاره ثلاث مرّات على عملي أنا.** في كلّ مرّة دُفع الثمن من
ملفٍّ آخر بدل رفع السقف:

| | |
|---|---|
| كود الذكاء الاصطناعي في `main.gd` | ‎+172 سطر ← رُفع إلى `ai_bridge.gd` |
| صفحة إعدادات AI | ‎+2 سطر ← موجّه الإعدادات رُفع إلى `settings_router.gd` |
| ملء الدوران في `room360.gd` | ‎+38 سطر ← رُفع إلى `room360_ai.gd` |

وتغيّرت قاعدة الحارس مرّة: سقفٌ مثبّت على العدد الحالي بالضبط يجعل كلّ سطرٍ
يُضاف بناءً فاشلاً، وفحصٌ يسقط لأنّ تعليقاً أُضيف هو فحصٌ يُعطَّل — ثمّ لا يكون
موجوداً يوم يهمّ. فصار هناك هامش أربعين سطراً، والقاعدة الصلبة هي **مجموع
الدَّين**: ملفّ يكبر، ويُدفع ثمنه من ملفٍّ آخر. المجموع اليوم ٥٧٢٩ نازلاً من
٦٨١٦ في المرحلة ٨٩.

---

## ما لا أستطيع التحقّق منه

**لا Godot أمامي ولا شبكة في هذه الآلة.** ستّة فواحص تمرّ، واختبارات
`tests/test_ai.gd` تغطّي الحمولة والترميز والتحويل في الاتجاهين وقراءة الجواب
من البايتات وإدخال الوضعيات المولَّدة — **كلّها بلا شبكة ولا مفتاح**. وما لا
يغطّيه أيّ اختبار هنا هو هل تُجيب Hugging Face، فتلك ليست خاصيّةً في هذا الكود.

**وأمرٌ يجب أن تعرفه قبل أن تجرّب:** FLUX.1-schnell على هذا العنوان هو نموذج
**نصّ إلى صورة**. الحمولة تُرسل حقل `image` و`strength` كما طلبتَ تماماً،
والكود يقرأ الجواب سواء عاد صورةً خاماً أو JSON فيه base64 — لكن إن تجاهل
الطرف الآخر الصورة، عاد إليك رسمٌ من الوصف وحده لا تحسينٌ لرسمك. إن حدث ذلك
فالمشكلة ليست في الأنابيب: بدّل `ENDPOINT` (السطر ٣٠) إلى نموذجٍ يدعم
image-to-image — `FLUX.1-Kontext-dev` مثلاً — وكلّ ما عداه يبقى كما هو.

```
python3 tools/check_all.py .                            # ٦ من ٦
godot --headless --path . --script tests/run_tests.gd   # ستّ حزم
bash tools/build.sh --release
```
