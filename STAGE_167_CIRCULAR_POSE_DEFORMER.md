# Stage 167 — Circular 360° Pose Deformer

## الهدف

تضيف هذه المرحلة نواة C++ اختيارية لتسهيل الانتقال بين الوضعيات حول دورة كاملة. وهي لا تستبدل `BoneRig` أو `RigSkin` أو `IvoryRig25D`، بل تعمل كطبقة تصحيح بعد أن يحسب نظام العظام الوضعية.

## ما تمت إضافته

- `IvoryPoseDeformer` في:
  - `native/ivory/src/ivory_pose_deformer.h`
  - `native/ivory/src/ivory_pose_deformer.cpp`
- مزج دائري للزوايا من 0 إلى 360 درجة.
- انتقال صحيح عبر الحد `359° → 0°`.
- منحنى C2 من نوع smooth quintic لتقليل القفزات بين المراجع.
- تصحيحات وضعية لكل رأس Mesh عبر `set_corrective(reference, deltas)`.
- إصلاح اختياري لأطوال حواف الشبكة لمنع انكماش المفاصل.
- حدود آمنة لقوة التصحيح وعدد دورات الإصلاح.
- تسجيل الصنف في `register_types.cpp`.
- جسر اختياري في `scripts/native.gd` عبر `Native.pose_deformer()`.
- اختبار مستقل في `tools/test_pose_deformer.py`.

## API الأساسي

```gdscript
var d: Object = Native.pose_deformer()
if d != null:
    d.set_reference_angles(PackedFloat32Array([0.0, 45.0, 90.0,
        135.0, 180.0, 225.0, 270.0, 315.0]))
    d.set_volume_mesh(rest_vertices, edges)
    d.set_corrective(2, side_pose_vertex_deltas)
    var posed: PackedVector2Array = d.deform(
        current_vertices, current_yaw_degrees, 1.0, 0.55, 2)
```

كل تصحيح هو فرق `delta` لكل رأس مقارنة بالشكل الأساسي. عند زاوية بين مرجعين، يوزع المحرك التصحيح بينهما. لا تُخزّن النواة الصور نفسها؛ الصور المرجعية تبقى مملوكة لـ `character360_state` كما هو في IVORY.

## طريقة الاستخدام مع IVORY

```text
BoneRig / IvoryRig25D
        ↓
الوضعية الهندسية الحالية
        ↓
RigSkin أو Mesh الحالي
        ↓
IvoryPoseDeformer (اختياري)
        ↓
Volume repair محدود
        ↓
العرض
```

يجب ألا يكون هناك solverان يكتبان إلى نفس الرؤوس في الوقت نفسه. العظام هي مالك الوضعية، وهذه النواة تضيف تصحيحًا وضعياً فقط.

## كيف يتم إعداد 360 درجة؟

ينبغي للفنان توفير مراجع حقيقية، مثل:

```text
0° أمام
45° أمام-يمين
90° يمين
135° خلف-يمين
180° خلف
225° خلف-يسار
270° يسار
315° أمام-يسار
```

لا يمكن للنواة اختراع تفاصيل الجهة الخلفية من صورة أمامية. الصور المرجعية تصنع المعلومة، والمزج الدائري يملأ المسافة بينها.

## إصلاح الحجم

`set_volume_mesh()` يستقبل نقاط الوضعية الأصلية وقائمة حواف. في كل تمريرة، يقارن المحرك طول الحافة في الوضعية الحالية بطولها الأصلي ويعيد جزءًا من الفرق. هذا يمنع انهيار المفاصل، لكنه ليس بديلًا عن تصميم شبكة جيدة أو تصحيح يدوي خاص بالكتف والكوع والوجه.

الإعداد الابتدائي المقترح:

```text
corrective_strength = 1.0
volume_strength     = 0.55
volume_passes       = 2
```

لا ترفع عدد التمريرات بلا قياس؛ فالإصلاح القوي قد يجعل الرسم صلبًا أو يتعارض مع حركة مقصودة.

## السلامة والتوافق

- إذا لم تكن مكتبة C++ مبنية، تبقى `Native.pose_deformer()` بقيمة `null` ويستمر المسار الحالي.
- لا تُعدّل هذه المرحلة بيانات الطبقات أو `project.json` تلقائيًا.
- لا تُخفي سطح الطبقة ولا تنشئ جلدًا جديدًا وحدها.
- يجب ربطها لاحقًا من غرفة 2.5D بعد اختبار التراجع والحفظ والتصدير.
- المستودع المرفق لا يحتوي `godot-cpp`، لذلك لم يُدّعَ بناء GDExtension كامل في هذه البيئة.

## التحقق

شغّل:

```bash
python3 tools/test_pose_deformer.py
```

وللبناء الكامل:

```bash
cd native/ivory
git clone -b 4.4 https://github.com/godotengine/godot-cpp
auto_scons="scons platform=linuxbsd target=template_debug"
# نفّذ الأمر أعلاه بعد تثبيت godot-cpp وSCons.
```

## ما لم يتم تفعيله تلقائيًا

لم يتم ربط النواة مباشرة بزر بناء 2.5D أو بخط العرض الحالي في هذه المرحلة؛ هذا مقصود لتجنب التأثير على المشاريع القديمة. الخطوة التالية الآمنة هي إضافة خيار تجريبي باسم **Pose Correctives 360°**، يعمل على نسخة من الطبقة وبعد توفر زر تعطيل واضح.

## ملاحظة الترخيص

الكود المضاف جزء من مشروع IVORY المرفق. يجب الاحتفاظ بتراخيص وتبعيات Godot وgodot-cpp عند بناء وتوزيع نسخة تجارية.
