# المرحلة ٩٧ — عظمةٌ واحدة، في مكانها الصحيح

## ١ · لم تكن عندك أداة عظام. كان عندك اثنتان

`bspline_room.gd` كان فيه `class Bone` خاصّ به. و`rig360.gd` كان فيه
`class Bone` **آخر**، مختلف. نسختان من شيءٍ واحد تتباعدان دائماً، وهاتان
تباعدتا فعلاً:

| | غرفة B-Spline | غرفة ٣٦٠ |
|---|---|---|
| الوصول المغلق (IK) | موجود | **غير موجود** |
| تقييد ٩٠° | طرف واحد | **الطرفان** |
| السلسلة الأبوية | موجودة | **غير موجودة إطلاقاً** |

فمن ينتقل بين الغرفتين كان ينتقل بين أداتين تبدوان واحدة ولا تتصرّفان
تصرّفاً واحداً. هذا هو سبب الشعور بأنّ «العظام ليست هي نفسها».

**الآن ملفّ واحد: `scripts/bone_rig.gd`.** صنف عظمة واحد، وحسابٌ واحد
تناديه الغرف الثلاث كلّها. وأيّ تصحيحٍ يُجرى فيه هو تصحيحٌ في كلّ غرفة
دفعةً واحدة.

## ٢ · الخلل الحقيقي وراء «العظام لا تبقى ثابتة داخل الرسمة»

لم تكن المشكلة في **مكان** العظام. كانت في `_carry_children`:

```gdscript
var shift: Vector2 = parent.b - parent.rest_b
child.a = child.rest_a + shift      # ← إزاحة
child.b = child.rest_b + shift      # ← إزاحة، لا دوران
```

الابن كان **يُزاح** بمقدار ما تحرّك طرف الأب. والإزاحة ليست دوراناً.

أدِر العضد ٩٠°: يصل الساعد إلى المرفق وهو **ما يزال يشير كما رُسم**،
خارجاً من الذراع جانباً، واللحم في وادٍ والعظم في وادٍ. هذا بالضبط ما
رأيتَه ووصفتَه.

البديل هو الحركة الأمامية (Forward Kinematics)، وهي ليست تحسيناً — هي
**ما تعنيه كلمة هيكل**: مكان الابن هو طرف أبيه، واتجاه الابن هو اتجاه
أبيه زائد دورانه هو. الدوران ينتقل عبر السلسلة لأنّ الدوران هو الذي
ينتقل.

**مُتحقَّقٌ منه بالأرقام** في `tests/test_bone_rig.gd`:

```
طرف الساعد بعد إدارة العضد ربع دورة:
  الإزاحة القديمة  →  (80, 100)   ← خارج من الذراع
  الدوران الجديد   →  (0, 180)    ← في امتداد الذراع
```

## ٣ · الـ ٩٠° — حُذفت من كلّ مكان

كانت في موضعين، `bspline_room.gd:841` و`rig360.gd:480`، وكلاهما:
`round(angle / (TAU/4)) * (TAU/4)`.

حُذف معها:

- الضغطة المزدوجة على المفصل التي كانت تُبدّله (إيماءةٌ تُغيّر سلوك السحبة
  التالية بصمت أسوأ من الميزة التي تصل إليها)
- المربّع الذي كان يُرسم للمفصل المقيَّد — فدليلٌ بمدخلٍ واحد ليس دليلاً
- الصنف `Bend` بأكمله

**والاختبار:** مئتا سحبة صغيرة أعطت **مئتَي زاوية مختلفة**. لو بقي
التقييد لأعطت أربعاً.

وقد راجعتُ البرامج الأخرى قبل الحذف كما طلبت: **Spine** يجعل الدوران حرّاً
افتراضاً ويطلب مفتاح `shift` للتقييد. **وعلى الجهاز اللوحي لا يوجد مفتاح
يُضغط.** فالتقييد الذي لا يمكن رفعه ليس ميزة، إنّه عرَج.

## ٤ · العظام انتقلت إلى الطبقة

كانت العظام رابع زرٍّ في صفٍّ من خمسة، داخل غرفةٍ بُنيت لأدوات المنحنيات.
فمن أراد هيكلاً كان عليه أن يعرف أنّ طريق العظمة يمرّ بأداةٍ لا شأن له بها.

المسار الآن هو الذي وصفتَه بالحرف:

> إعدادات الطبقة ◂ **«أضف عظاماً»** ← تُفتح الطبقة **وحدها** ← تضع العظام
> ← **«تطبيق»** ← تُدمج في الرسم ← تُحرَّك بسحب **مفاصلها** على اللوحة

ملفّان جديدان:

- **`scripts/ui/bone_room.gd`** — الغرفة. الطبقة وحدها على شبكةٍ مقيسة،
  بإطارٍ ذهبيّ حول حدّ الرسم، وثلاثة أوضاع لا رابع: أضف عظماً، حرّك مفصلاً،
  احذف عظماً. ثمّ «تطبيق».
- **`scripts/ui/bone_handle.gd`** — التحريك على اللوحة نفسها. كانت
  `canvas_marks` ترسم المفاصل منذ مراحل، ولم يكن شيءٌ على اللوحة يستمع
  للمسةٍ تقع عليها: كانت **صورةَ** أداةٍ لا أداة.

وأُزيل زرّ «أضف عظماً» من غرفة B-Spline. وبقي فيها **التحريك**، لأنّها
تجمع مجلّداً كاملاً في هيكلٍ واحد، وهذا ما لا تستطيعه ورقة إعدادات الطبقة.

### ولا يُكتب شيءٌ في الرسم أثناء التحريك

نائبٌ شبكيّ (`RigSkin`) يقف أمام الطبقة وينحني، والطبقة خلفه مخفيّة وسليمة.
فلا توجد نسخةٌ من هذا قادرةٌ على إتلاف الرسم.

## ٥ · «تبقى ثابتة داخل الرسمة» — القاعدة التي تُنفّذها

`bone_room._move_joint` يرفض أيّ تصحيحٍ يقع خارج الحبر. فلا يستطيع أيّ
قدرٍ من السحب أن يمشي بعظمةٍ إلى ورقٍ فارغ، والهيكل الموضوع على شخصيةٍ يبقى
على تلك الشخصية.

وكلّ طرفٍ عند المفصل يتحرّك معه، لا الطرف الذي أمسكته اليد وحده — فعظمتان
تلتقيان عند مرفقٍ هما مفصلٌ واحد في نظر كلّ من ينظر إليه.

## ٦ · Character 360 صارت تنحني فعلاً

`_draw_figure` كان يرسم الشخصية عبر `TurnMorph`، والعظام تُرسم **فوقها**
كزينة. تسحب العظمة فتتحرّك العظمة والرسم واقف.

`TurnMorph.build` يُعيد الشخصية شبكةً أصلاً — لأنّ الدوران نفسه تشويه. فصار
الانحناء تمريرةً إضافية على رؤوسٍ تُصنع على أيّ حال، لا آلةً ثانية بجانب
الأولى. والوزن هو `RigSkin.bone_rule` نفسه الذي تستخدمه اللوحة، فالطرف
المنحني على الدوران ينحني كما ينحني تماماً على الطبقة.

**والفضاءان:** شبكة المسخ في فضاء الشخصية (القدمان عند الأصل، الوحدة هي
الطول القياسي)، والعظام في فضاء الصفحة. الفرق بينهما هو تلك الوحدة بالضبط،
وهي مكتوبةٌ مرّةً واحدة في `BoneRig.bend_mesh`. عكسُها هو الفرق بين شخصيةٍ
تنحني وشخصيةٍ تطير خارج الشاشة.

**ولا يكلّف شيئاً إن لم تتحرّك عظمة:** هيكلٌ ساكن يُعيد الشبكة كما هي.
فالشخصية ذات الهيكل لا تدور أبطأ من الشخصية بلا هيكل.

## ٧ · ما هو محسوبٌ لا مُدَّعى

`tests/test_bone_rig.gd`، خمس حالات، مُسجَّلة في `run_tests.gd`:

1. **السلسلة تنقل الدوران** — الحالة أعلاه بالرقمين
2. **الأطراف تحفظ أطوالها** — ١٥٦ وضعية، ولا عظمة تمدّدت أو انكمشت
3. **لا شيء يقفز** — ٢٠٠ سحبة، ٢٠٠ زاوية
4. **الوضعية تنجو من الحفظ والفتح** — بما في ذلك ملفٌّ قديم لا يحمل `turn`
   ويحمل `hinged`: تُستعاد الوضعية من مواضع الأطراف وحدها، فلا يخسر أحدٌ
   عملاً أنجزه
5. **العظمة لا تكون جدّةَ نفسها** — حلقةٌ مغلقة تُفتح بدل أن يُمشى فيها
   إلى ما لا نهاية

## ٨ · الميزانية

`bspline_room.gd` نزل **٢٣٦ سطراً** (٢٣٢٩ ← ٢٠٩٣). صنفا عظام صارا واحداً،
وخرج منه `_swing` و`_carry_children` و`_reach_to` و`_elbow_side`
و`_join_reach` و`_joint_at` — نحو مئةٍ وثمانين سطراً، لكلّ سطرٍ منها توأمٌ
في غرفة ٣٦٠ كان قد اختلف عنه قليلاً.

وذلك ما **دفع ثمن** نموّ `main.gd` و`canvas_view.gd` في البناء نفسه. سجلّ
`tools/check_size.py` أُعيد تسجيله، والدَّين الكلّي نزل من ٥٧٩٥ إلى ٥٦٤٠.

المدقّقات الثمانية كلّها تمرّ.

## ٩ · شيءٌ ينبغي أن تعرفه

`bone_room` يضع الهيكل ولا يُحرّك. التحريك يقع على اللوحة بعد «تطبيق». هذا
الفصل هو سبب صغر الغرفة — وهو أيضاً ما طلبتَه: «يتم تحريكها عن طريق النقاط
خاصتها».

ولم أستطع تشغيل Godot هنا لأُجرّب البناء بعينَيّ. ما جرى تشغيله هو
المدقّقات الثمانية، ومنطق العظمة مُحاكىً عددياً خارج المحرّك قبل كتابته.
فإن ظهر خطأ صياغة عند الفتح، أرسل الصورة كما تفعل دائماً ويُصلَح فوراً.
