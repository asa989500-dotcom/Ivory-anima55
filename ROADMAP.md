# خارطة الطريق — ما اتُّفق عليه ولم يُنفَّذ بعد

هذا الملفّ سجلّ، لا وعد. كلّ بندٍ فيه مطلوب، وكلّ بندٍ **لم يُبنَ بعد** حتى
يُنقَل إلى ملفّ مرحلةٍ باسمه. ولا يُشطب بندٌ إلّا حين يعمل فعلاً في التطبيق —
لأنّ أسوأ ما وجدناه حتى الآن (التظليل المنقّط) كان ميزةً تبدو مكتملة من كلّ
جهةٍ يستطيع إنسان أن ينظر منها، ولا تفعل شيئاً.

## ١ · الأداء وإدارة الذاكرة

- [ ] **تخزين الأطر المؤقّت** (Frame Caching). الحالة الآن: `PaintSurface`
      فيه نظام بلاطات ونوم/يقظة و`Perf` فيه مستويات، لكن لا شيء يخزّن
      **الإطار المسطَّح** — كلّ إطارٍ يُعاد تسطيحه.
- [ ] **تقليل دقّة العرض أثناء التشغيل السريع** (Dynamic Viewport Scaling)،
      وإعادة الدقّة الكاملة عند التوقّف. `Perf.headroom` موجود ويُقرأ الآن في
      `puppet_warp` — وهذا هو المدخل نفسه.

## ٢ · الرسم والفرش

- [ ] **محرّك مسارات متجهة** قابل للتعديل بالنقاط. الموجود: `bspline.gd`
      و`spline_anchor.gd` — بنية المنحنيات قائمة، والناقص أن تكون **طبقةً**
      تُرسم بها لا أداةً في غرفة.
- [x] **تنعيم الخطّ المباشر** (Stabilization). **المرحلة ١١٢.** وكان هنا
      اكتشافٌ يستحقّ التسجيل: `StrokeSmoother` فيه مرشّح One Euro يعمل منذ
      زمن — لكنّه يزيل **الرجفة**، وهي ليست ما يعانيه الإصبع السريع على
      الزجاج. الإصبع السريع لا يرتجف، بل **يتموّج** في أقواسٍ طويلة ضحلة،
      والمرشّح يمرّرها لأنّها عنده تشبه منحنىً مقصوداً تماماً.
      فأُضيفت مرحلةٌ ثانية — المقود: السنّ يُجرّ خلف الإصبع، فتقطع المسحة
      السريعة زاوية كلّ تموّج. آليّةٌ مختلفة لا رقمٌ أكبر في الأولى. ومؤشّر
      **«ثبات اليد»** في لوحة الفرشاة، والتأخّر مذكورٌ فيه صراحةً كثمن.

## ٣ · الشريط الزمني

- [ ] **ورقة التوقيت / X-Sheet**: تمديد الإطار (Hold) إلى 2s أو 3s بضغطة،
      بلا تكرارٍ يدوي.
- [ ] **كاميرا متعدّدة المستويات** (Multi-Plane) لعمقٍ بصريّ بين الخلفية
      والواجهة.
- [ ] **ترميز الطبقات والإطارات بالألوان**: تمييز التخطيط (Rough) عن التحبير
      (Clean-up).

## ٤ · التصدير والتكامل

- [ ] **تسلسل PNG شفّاف** و**EXR** للتجميع في برامج أخرى.
- [ ] **حفظ خيارات التصدير الشائعة تلقائياً**.

## ٥ · التنظيف والمراجع واللمس

- [x] **بصلة بلونين**: موجودة أصلاً وتعمل — `AnimPlayer.ONION_BACK` أحمر
      و`ONION_FORWARD` أزرق، ولونان قابلان للتغيير من الشريط الزمني، ويُطبَّقان
      فعلاً في `_ghost_at`. فُحصت ولم تُبنَ، ولم أضف إليها شيئاً.
- [ ] **مراجع تخطيط سريعة** (Layout Blockers) تُرسم فوقها أطر 2D.
- [ ] **مراجعة الإيماءات اللمسية**: التقريب والتدوير والمسح.

## ٦ · الأدوات

- [x] **أداة تحريك عامّة بلا تحديد**. **المرحلة ١١٢** — `ui/move_tool.gd`،
      على زرّ «حدث الإدخال». تُمسك رسم الطبقة كما هو، وتُحرَّك وتُكبَّر
      وتُصغَّر بلا تحديدٍ مسبق. والتكبير يُحسب **من الأصل في كلّ مرّة** لا من
      النتيجة السابقة، فالبحث عن المقاس دقيقةً كاملة لا يخسر شيئاً. ولا تُدير:
      إدارة الحبر إعادة تشكيلٍ بزاوية تُلين كلّ خطّ، ومقبضٌ يعد بذلك صامتاً
      وعدٌ لا تستطيع الأداة الوفاء به.

## ٧ · مشروع الحياكة — ما بُني وما لم يُبنَ

**بُني في المرحلة ١١٤:** صيغة المشروع ولونه على الرفّ، ونموذج البيانات
(دبابيس وخيوط ولفّات)، والدبوس بضغطتين وسحب، واللفّ من دبوسٍ إلى دبوس،
والخيوط الثمانية مرسومةً ببرمها، وعجلة الـW مع مربّعات التدرّج، وألوان
الدبابيس، والبناء فوق/تحت/متعرّج، والتراجع والإعادة، والحفظ الفوريّ، والكانفاس
اللانهائيّ الوهميّ.

**وبُني في المرحلة ١١٥:**

- [x] **استيراد صورة** وخليّة `IM` وصندوق تحويلها، ومحو الصورة وحدها.
- [x] **أنواع التصدير** المخصّصة، والتصدير الذي يقيس آخر مسافة بُني فيها
      ويكبّر ليسع الصورة.
- [x] **نزع الدبابيس** في النهاية وتشابك الخيوط بمرونة.
- [x] **التصدير إلى مشروع رسوم متحرّكة** كطبقة `wool character` بعظام.
- [x] **لفّ الخيط حول الدبوس مرّتين** كإيماءةٍ صريحة (الآن تُستنتج اللفّة
      الثانية من وجود خيطٍ مربوطٍ عند ذلك الدبوس). صارت إيماءةً: دوّر إصبعك
      حول الدبوس قبل أن ترفع.
