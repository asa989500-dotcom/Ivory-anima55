# المرحلة ٩٢ — إصلاح ما ظهر في الصور، ثم مشكلة الاتساق

## أولاً: لماذا لم يُترجَم المشروع

ثمانية أخطاء في `main.gd` كانت كلّها صدىً لعطلٍ واحد في مكانٍ آخر:

```
res://scripts/ai_bridge.gd:31, 92, 142   There is already a parameter named "p"
res://scripts/ui/image_import.gd:73      There is already a parameter named "p"
```

أداة النقل `lift_module.py` تسمّي المعامل الأول `p` دائماً. وثلاث من الدوالّ
المنقولة كانت تصرّح أصلاً `var p: ProjectManager.Project`. وGDScript لا يقبل
متغيّراً محلّياً باسم معاملٍ موجود — فسقط الملفّان، ثمّ سقط معهما كلّ ملفٍّ
يذكر `AiBridge` أو `ImageImport`. **ثمانية أخطاء من تصادمٍ واحد، في ملفّاتٍ
لا عيب فيها.**

والأسوأ: الكود لم يكن مجرّد متصادم الأسماء، بل **خاطئ المعنى**. بعد سطر
`var p: ...` صار كلّ `p` يعني المشروع، بينما كانت الأداة قد كتبت `p._flash(…)`
تقصد الغرفة. أي أنّ الدوالّ كانت ستستدعي شيئاً على الكائن الخطأ لو ترجمت.

**الحلّ:** الكائن المضيف صار يُبلَغ عبر `host`، والمشروع بقي `p`. والتمييز لم
يكن بالعين: `host.` تخصّ أعضاء `MainRoom` و`p.` تخصّ `stack` و`page` وحدهما،
وهما مجموعتان منفصلتان تماماً، فأُجري الفصل آلياً ثمّ دُقّق.

## وفواحصي — لماذا لم تمسك هذا

`check_shadow.py` وُجد ليمسك بالضبط هذا الصنف من الخطأ، **وقرأ الملفّات
الأربعة وقال إنّها نظيفة**. لأنّه كان يبدأ النظر بعد سطر التوقيع: المعاملات لم
تكن محسوبة كمتغيّرات محلّية أصلاً.

أُصلح. وأُثبت بإدخال العطل عمداً:

```
tests/_probe.gd:4: "p" clashes with the parameter at line 3 (in broken)
```

**وأداة النقل نفسها:** صارت تختار اسم المضيف بمقارنته بما يستعمله الكود
المنقول فعلاً، وتتوقّف إن كانت كلّ الأسماء مأخوذة — بدل أن تُخرج شيئاً يبدو
صحيحاً. وصارت أيضاً لا تُعطي مضيفاً لدالّة لا تستعمله: `_premultiply` كانت
حساباً على صورة ولا شيء غيره، وأُعطيت معاملاً لا حاجة لها به فاضطُرّ كلّ
مُستدعٍ لتمريره بلا سبب.

## بقيّة ما في الصور

| ما ظهر | ما كان | ما صار |
|---|---|---|
| ٦ تحذيرات `UNUSED_PRIVATE_CLASS_VARIABLE` | المتغيّرات تُقرأ من الوحدات المرفوعة، والمترجم لا يرى `host._improving` راجعاً إلى هنا | `@warning_ignore` على كلّ تصريح وحده، لا على الملفّ — فمتغيّر يموت فعلاً يبقى يُبلَّغ عنه |
| `"ready" is shadowing a signal` | متغيّر محلّي اسمه `ready` في `finish_transfer` | صار `prepared` |
| ١٠ × `Integer division` | قسمة صحيحة في `perf.gd` و`perf_bench.gd` | كُتبت بالعوائم صراحةً؛ والثلاث المقصودة فعلاً (فهرس ← صفّ) أُشّرت بـ `@warning_ignore` |
| `2D MSAA is not yet supported for GLES3` | `msaa_2d=2` مع `gl_compatibility` | صار `0` — **لم يكن يفعل شيئاً أصلاً**، فقط يطبع سطراً في كلّ إقلاع يجب تجاوزه للوصول إلى خطأٍ حقيقي |

---

## ثانياً: الاتساق — «نفس الشخصية، لا شخصية تشبهها»

قلتَ إنّها أصعب نقطة، وأوافقك. وأقول لماذا لا يحلّها نموذجٌ جيّد وحده: اطلب
«فارس بدرع فضي» مرّتين تحصل على فارسين. كلاهما جيّد، ولا أحد منهما هو الآخر.
لرسمةٍ واحدة هذا مقبول، ولرسومٍ متحركة عديم النفع — شخصيةٌ تكون إنساناً مختلفاً
قليلاً في كلّ لقطة ليست شخصية.

`scripts/ai_identity.gd` يثبّت أربعة أشياء، كلّ واحدٍ منها يسدّ باباً مختلفاً
للانحراف:

**البذرة (seed).** الرقم نفسه يعطي النموذج الضوضاء الابتدائية نفسها، فتنزل
الكلمات نفسها على الوجه نفسه لا على وجهٍ جديد. متروكةً للصدفة تعني شخصاً جديداً
في كلّ طلب؛ مثبّتةً تعني الشخص نفسه يُطلب منه شيءٌ آخر. وهذه وحدها معظم المعركة.

**العمود (spine).** الجملة التي تُرفَق بكلّ طلب مهما كان: مَن هذا، ماذا يلبس،
ما أسلوب الرسم. تُكتب مرّة ثمّ تُلحَق تلقائياً — لأنّ ما يحدث فعلاً بدونها أنّ
الطلب العاشر يقول أقلّ من الأوّل، فتبسط الشخصية نفسها بهدوء.

**اللوحة اللونية.** تُقرأ من **رسم الفنان** أرقاماً، وتُذكر في الطلب. «درع فضي»
أمنية؛ وستّة ألوان محدّدة أمر — وهي ما يمنع الدرع من أن يصير ذهبياً في اللقطة
الرابعة.

**المرجع — يُعاد قراءته بعد كلّ تعديل.** وهذا أهمّها وأسهلها خطأً، وهو بالضبط
ما وصفتَه: لو ولّد الذكاء الاصطناعي شخصية ثمّ عدّلها الفنان ثمّ طلب زاوية أخرى،
يجب أن ينطلق الطلب من **الرسم كما هو الآن**، لا ممّا أنتجه النموذج قبل أن
يعترض عليه الفنان. وإلّا فكلّ تصحيحٍ يُلغيه الطلب التالي، ويصير الفنان يجادل
أداته.

الترتيب مقصود: **رسم الفنان يعلو على ذاكرة النموذج، دائماً.** وهذا هو موقف
الميزة كلّها — الذكاء الاصطناعي مساعد، والفنان هو مَن يُساعَد.

### أين تعمل

- **غرفة التصميم:** مربّع «أبقِها الشخصية نفسها»، مفعّل افتراضياً. أوّل وصفٍ
  يؤسّس الشخصية، وكلّ ما بعده يُطلب منها هي. واللوحة تُعاد قراءتها من كلّ صورة
  تعود، فالطلب الرابع مربوطٌ بالصورة الثالثة لا بالوصف الأوّل — الشخصية تنحرف
  درجةً واحدة في كلّ مرّة وإلّا.
- **تحسين الطبقة:** اللوحة تُقرأ من تلك الطبقة، الآن، قبل الإرسال.
- **دوران ٣٦٠:** هويّة واحدة لكلّ الطلبات الثمانية — البذرة نفسها والعمود نفسه
  واللوحة المقروءة من الوضعية الأمامية المستوردة. ثمانية طلبات، كلٌّ منها
  ينطلق من ضوضائه الخاصة، تعطي ثماني شخصيات يشبه بعضها بعضاً، وهو الفشل نفسه
  بعينه.
- **الاستيراد:** صورةٌ تُدخلها من جهازك تصير الشخصية. هذا هو باب «هذه رسمتي —
  أعطني إيّاها من الجانب».

والهويّة تُحفظ مع المشروع، **والبذرة قبل كلّ شيء**: ضياعها يعني ألّا تستطيع
رسم الشخصية مرّة أخرى أبداً.

---

## ما لا أستطيع التحقّق منه

لا Godot ولا شبكة أمامي. الفواحص الستّ تمرّ، والاختبارات تغطّي بناء الطلب
وقراءة اللوحة وثبات البذرة وحفظ الهويّة واسترجاعها — بلا شبكة ولا مفتاح. أمّا
هل يلتزم النموذج بما أُعطي، فتلك مسألته لا مسألة هذا الكود.

```
python3 tools/check_all.py .                            # ٦ من ٦
godot --headless --path . --script tests/run_tests.gd
```
