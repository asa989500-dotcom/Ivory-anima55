# المرحلة ١٠٩ — حذف Character 360، وتقوية العظام وpuppet warp

## ١ · Character 360 — حُذفت كلّها، لا أثر لها

### الملفّات المحذوفة

| الملفّ | الأسطر |
|---|---|
| `scripts/character360.gd` | ٩٠٣ |
| `scripts/rig360.gd` | — |
| `scripts/turn_morph.gd` | — |
| `scripts/ai_turnaround.gd` | — |
| `scripts/ui/room360.gd` | ١١٩٤ |
| `scripts/ui/room360_ai.gd` · `room360_dangle.gd` · `room360_keys.gd` | — |
| `scripts/ui/compass_360.gd` · `grid_360.gd` · `onion_360.gd` | — |
| `tests/test_rig360.gd` | تسع حالات |
| `assets/icons/character360.png` | الأيقونة |

### والشيفرة التي كانت تشير إليها، في خمسة عشر ملفّاً

`main.gd` — عشر دالّات (`enter_360_room`، `_leave_360_room`،
`_take_360_character`، `_stamp_360*`، `_place_360_selection`،
`_hold_360_stroke`، `_turn_360_layer`، `_ring_mask`) وصفّ البوصلة الذي يفتحها.

`canvas_view.gd` — إشارتا `stroke_finished` و`selection_on_360` ووسيطا الرسم.
`canvas_marks.gd` — فرع الـ360 في تثبيت التحديد، وفرعه في رسم الهيكل،
و`draw_360_rig` و`sel_360_index`.
`layer_stack.gd` — الحقول `is_360`، `character`، `rig_360`، `turn_360`،
`scale_360`، `place_360`، `show_points`، `show_rings`، وقاعدة `frames_locked`،
ونسخها في `duplicate`.
`vault.gd` — ستّة حقول في الكتابة وستّة في القراءة.
`rig_track.gd` — `along` و`dials` و`rings` و`turn`، و`take_rig360`
و`take_turn` و`lay_rig360` و`lay_rig360_pose`، ومزجها في `_blend`.
`anim_player.gd` · `bone_rig.gd` · `ai_assistant.gd` · `tool_panels.gd` ·
`layers_panel.gd` · `bone_handle.gd` · `layer_rig_row.gd` ·
`timeline_panel.gd` · `lang.gd` · `tests/test_ai.gd` · `run_tests.gd`.

**والنتيجة:** `grep -r "360"` على كلّ ملفّات `.gd` لا يُرجع اسماً واحداً.
والفواحص التسع تمرّ. و`main.gd` نزل من ٢٦٠٤ سطراً إلى ٢٣٠٢،
و`layer_stack.gd` من ١٣٩٩ إلى ١٣٧٢، و`tool_panels.gd` من ١٣٤١ إلى ١٢٩٩ —
والدفتر في `check_size.py` **نُزِّل إلى الأرقام الجديدة**، لأنّ دفتراً يُبقي
سماح الأمس بعد أن انكمشت الشيفرة ليس دفتراً بل سقفاً لا يضغط أحداً.

### ثلاثة أشياء أُنقذت بدل أن تُحذف معها

**ألوان العظام الثلاثة** (`BONE_INK`، `BONE_CORE`، `BONE_EDGE`) كانت تعيش في
`Rig360`، و`canvas_marks` يقرؤها منه لرسم **كلّ** هيكل في التطبيق. حذفها كان
سيكسر رسم العظام العادية. نُقلت إلى `BoneRig` نفسه، بجانب العظمة.

**`Bone.built_on` و`built_height` و`carry`** حُذفت لأنّها لا معنى لها خارج
الدوران — و`bend_mesh` صار أنظف بثلاثة أسطر.

**الحركة الثانوية.** أهمّ ما وُجد: `BoneSolver.settle` مكتوب ومُختبَر ولم
يستدعِه شيء إلّا عبر `rig_360`. أي أنّ حذف الـ360 كان سيحذف ميزة كاملة
بالخطأ. انظر القسم التالي.

---

## ٢ · الحركة الثانوية — من شيفرة ميتة إلى ميزة تعمل

الشعر والقماش والذيل تصل **بعد** الوضعية بلحظة، فتتبع الحركة بدل أن تكون
ملحومة بها. الرياضيات كانت موجودة كلّها. الناقص ثلاثة أشياء:

**أين تُحسب.** كان `_dangle` مروراً منفصلاً **بعد** `apply()` — أي أنّه
يُهدّئ هيكلاً رُسم من قبل، فالرسم متأخّر إطاراً كاملاً عن عظامه. الآن الخطوة
الزمنية تُسجَّل فقط، والعمل يجري داخل `_show_pose` حيث تتحوّل الوضعية إلى
شكل: مكان واحد يقرّر أين العظمة.

**على ماذا يعمل النابض.** المفتاح يحمل كلّ عظمة كمحور وزاوية **عالميّة**،
والزاوية العالمية تحوي كلّ دوران فوقها في السلسلة — فتهدئتها تجعل اليد تتأخّر
خلف الذراع **وخلف تأخّرها هي**، وكلّما نزلت في السلسلة زاد الانحراف. فتُوضَع
الوضعية على هيكل حقيقي و`BoneRig.adopt` يستخرج دوران كلّ عظمة **في إطار
أبيها**، وهي الكمّية الوحيدة التي يجوز تشغيل نابض عليها.

**لماذا يُحفَظ الهيكل بين الإطارات.** لأنّ النابض **هو** حالته: `eased`
و`eased_target` و`eased_speed`. هيكل يُعاد بناؤه كلّ إطار = تأرجح يبدأ من
الصفر ستّين مرّة في الثانية = لا تأرجح إطلاقاً. يُحفَظ بمعرّف الطبقة ويُسقَط
حين يتغيّر عدد العظام أو يتغيّر المشهد.

**ولا يعمل إلّا أثناء التشغيل.** شريط زمني يُسحب باليد يجب ألّا يُطلق تأرجحاً،
وإلّا بقيت الشخصية تتحرّك بعد رفع الإصبع — وهذا يُقرأ كعدم استقرار في الرسم لا
كفيزياء. والخطوة **مقيَّدة** بـ ١/٢٠ ثانية: إطار استغرق ربع ثانية (مشروع
يُفتح، التطبيق يعود من الخلفية) كان سيُدمَج كربع ثانية تأرجح، فيقفز كلّ شيء
سائب في اللحظة التي يتعثّر فيها التطبيق أصلاً.

### والمدخل إليها: وضع «سائب» في غرفة العظام

النابض يمزج بـ`Bone.soft`، ولم يكن في التطبيق مكان واحد يضبطه. أُضيف **وضع
رابع** إلى شريط غرفة العظام بجانب «أضف عظماً» و«حرّك مفصلاً» و«احذف عظماً»:

اضغط العظام التي هي شعر أو قماش أو ذيل، فتُرسم **بالأزرق** بدل العاجي — لأنّ
«هذه العظمة شعر» يجب أن تُقرأ عبر هيكل كامل بنظرة واحدة، ودرجة من اللون نفسه
لا تكفي. واضغط ثانية لتعود صلبة. والقيمة ثلاثة أرباع لا كلّها: خصلة تتبع
الحركة تماماً هي خصلة **لا تصل أبداً**، وشعر لا يستقرّ يُقرأ كخطأ لا كثقل.

و`soft` يمرّ عبر `to_rows`/`from_rows` أصلاً، فيُحفَظ في الملفّ ويعود.

### وفي إعدادات الطبقة

ثلاثة مؤشّرات — **الصلابة** و**الاستقرار** و**التجاوز** — تظهر فقط إذا كان
على الهيكل شيء سائب فعلاً. ثلاثة مؤشّرات لا تستطيع فعل شيء هي ثلاثة أسئلة بلا
جواب، فإن لم يكن هناك شيء سائب قِيل ذلك صراحةً مع الطريق إليه. وأُضيف مؤشّر
**إظهار الهيكل** (كان `show_bones` حقلاً بلا واجهة).

التجاوز تحت الصفر يتراجع قبل أن يتحرّك — وهذه قاعدة تحريك أقدم من الحواسيب.

---

## ٣ · الوصلة المفقودة: التوضيع على اللوحة صار يكتب مفتاحاً

**هذه أخطر ثغرة وُجدت في هذه المرحلة.**

`BoneHandle.release` كان يكتب العظام على الطبقة **ويقف**. والشيء الوحيد في
التطبيق كلّه الذي كتب **مفتاحاً** هو غرفة B-Spline. أي أنّ شخصية وُضِّعت حيث
تعيش كان لها وضعية واحدة — الأخيرة — وتحريك رأس التشغيل لا يفعل شيئاً.

العظام والشريط الزمني كانا ميزتين لا تعرف إحداهما الأخرى، والسطر الناقص
بينهما كان هذا. اثنِ الذراع عند الإطار ١، امشِ إلى الإطار ٢٠، اثنِها ثانية،
فيملأ التطبيق كلّ إطار بينهما: هذا **كلّ** تحريك العظام، وكان على بُعد نداء.

**ولا يُكتب مفتاح حيث لا يعني شيئاً.** المسار الفارغ لا يُعطى مفتاحاً وحيداً
عند الإطار ١٢ يقول «كما رُسمت» — مفتاح لا يغيّر شيئاً ويجب البحث عنه وحذفه
لاحقاً. بل يُوضَع **مفتاحان**: واحد عند الإطار صفر يحمل الرسم في راحته، وواحد
هنا يحمل ما فُعل للتوّ. وهذا ما يجعل أوّل انحناءة **حركةً** لا قفزة — تسافر
الشخصية من حيث رُسمت إلى حيث وُضِعت عبر كلّ إطار بينهما، دون أن يضطرّ أحد
لتمفتح البداية.

---

## ٤ · puppet warp — سرعة أثناء السحب، دقّة عند الرفع

مرّات الاسترخاء (`_relax_smooth` + `_relax_area`) هي الفرق بين انحناءة
ولطخة. وكان عددها **اثنتين دائماً**، وهو رقم خاطئ مرّتين: كثير على جهاز رخيص
يحاول ملاحقة إصبع بستّين إطاراً في الثانية، وقليل جدّاً في اللحظة الوحيدة التي
لا يهمّ فيها إلّا الجودة ولا ينتظر أحد.

فصارت سرعتين:

**أثناء السحب** — الجواب يجب أن يصل قبل الإطار التالي، وسيُستبدل بالحدث
التالي على أيّ حال. مرّة واحدة على جهاز بلا فائض، مرّتان على غيره
(`Perf.headroom`).

**عند رفع الإصبع** — لا شيء ينتظر، والشكل على وشك أن يُنظَر إليه. ستّ مرّات،
وهو ما بعد النقطة التي تُغيّر فيها مرّة إضافية شيئاً تجده العين.

**وعند `bake()`** — قبل أن يصير بكسلات تُكتَب في الرسم وتبقى. ستّ مرّات
أيضاً، لأنّها المرّة الوحيدة في الأداة كلّها التي تستحقّ فيها الدقّة أيّ وقت.

الحساب نفسه في الحالات الثلاث. هذا يشتري دقّةً بوقتٍ كان ضائعاً، ولا ينفق
وقتاً لم يكن كذلك.

---

## الملفّات

محذوفة: ثلاثة عشر ملفّاً (فوق).
معدّلة: `main.gd` · `canvas_view.gd` · `canvas_marks.gd` · `layer_stack.gd` ·
`vault.gd` · `rig_track.gd` · `anim_player.gd` · `bone_rig.gd` ·
`puppet_warp.gd` · `ai_assistant.gd` · `exporters.gd` · `lang.gd` ·
`ui/bone_room.gd` · `ui/bone_handle.gd` · `ui/layers_panel.gd` ·
`ui/layer_rig_row.gd` · `ui/tool_panels.gd` · `ui/timeline_panel.gd` ·
`ui/bspline_room.gd` · `ui/ai_room.gd` · `tests/test_ai.gd` ·
`tests/run_tests.gd` · `tools/check_size.py`.

---

## ٥ · `comic_panels.gd` — سبعة أخطاء، سببها اسم واحد

فتحتَ الملفّ في Godot فجاءت أربعة أخطاء وثلاثة تحذيرات. وكلّها من سطر واحد:

```
class Panel extends RefCounted:
```

`Panel` **صنف أصليّ في Godot** — عنصر `Control` يرسم خلفية. والصنف الداخلي
لا يجوز أن يأخذ اسماً أصليّاً.

### ولماذا كانت الثلاثة التالية أسوأ من الأوّل

الخطأ الأوّل يقول ما حدث. والثلاثة بعده لا تقول شيئاً:

```
Cannot assign a value of type Panel to variable "out" with specified type Panel.
```

**الاسمان مكتوبان بحروفٍ واحدة في الجملة نفسها.** لأنّ `Panel.new()` داخل
الملفّ صار يُحلّ إلى `Panel` **الخاصّ بالمحرّك**، فكلّ بانٍ هنا كان يبني
`Control`، بينما كلّ تصريح نوع ما زال يعني الصنف الداخلي. من يقرأ هذه الرسالة
يستنتج أنّ المترجم معطوب، ويبدأ بتغيير شيفرة سليمة. ولا شيء في الرسالة يشير
إلى التصريح الذي يبعد أربعين سطراً فوقها.

**الإصلاح:** `Panel` ← `Pane`، و`duplicate_panel` ← `duplicate_pane`،
وحُدّثت الإشارتان في `comic_board.gd` و`test_comics.gd`. والاسم الجديد
مشروح في الملفّ نفسه حتى لا يُعاد إلى `Panel` لاحقاً ظنّاً أنّه أوضح.

### والتحذيرات الثلاثة

```
Line 102/120/135 (SHADOWED_VARIABLE): "at" is shadowing an
already-declared function at line 156 in the current class.
```

`static func at()` في السطر ١٥٦، وثلاثة متغيّرات محلّية اسمها `at`. تحذير لا
خطأ — وهذا هو الفخّ: الملفّ يُبنى، فلا شيء يجبرك على معالجته. لكن الاسم داخل
تلك الدالّات لم يعد يعني الدالّة، ويوم يُضاف نداء `at(...)` في دالّة فيها
محلّيّ بالاسم نفسه، سيُحلّ إلى المحلّيّ ويفشل برسالة عن نداء شيء غير قابل
للنداء. الدالّة صارت `pane_at` — وهو اسم أوضح أصلاً.

---

## ٦ · ولماذا لم تُمسك الفواحص هذا؟ — فاحصان أُصلحا

هذا أهمّ ما في القسم. الفواحص التسع مرّت على الملفّ **وقالت إنّه نظيف**، وهو
لم يكن. فاحص يُقرأ ويقول «سليم» وهو مخطئ أسوأ من فاحص غير موجود.

### `tools/check_native_names.py` — جديد

يرفض كلّ `class_name` وكلّ `class` داخليّ يصطدم باسم صنف أصليّ في Godot.
مُجرَّب على النسخة **قبل** الإصلاح فأمسك السطر ٣٩ فوراً.

والقائمة فيه ليست كلّ أصناف Godot التسعمائة ولا تحاول أن تكون: هي كلّ اسم
أصليّ **قد يختاره إنسان فعلاً** — أسماء إنجليزية عادية مثل Panel وTimer وLabel
وCurve وImage وBone وCamera وLine. لن يُسمّي أحد صنفاً داخليّاً
`VisualShaderNodeVec4Parameter` بالخطأ. وهذا مكتوب صراحةً في رأس الملفّ: نجاحه
يعني «لا اصطدام باسمٍ كنتَ لتختاره»، لا «لا اصطدام بشيء في المحرّك» — وهذا هو
كلّ الخطر عملياً، لأنّ الاصطدامات التي تقع فعلاً هي الكلمات القصيرة العادية.

### `tools/check_shadow.py` — ثغرتان فيه

**الأولى: كان يقارن المحلّيّ بالمحلّيّ فقط**، لا بدالّات الصنف نفسه. صار
يمسكها. وعلى النسخة القديمة يُرجع خمس حالات: الثلاث في `comic_panels.gd`،
واثنتين في ملفّين حُذفا في هذه المرحلة.

**والثانية أخطر: التوقيع الملفوف كان غير مرئيّ له تماماً.**

```gdscript
static func _crosses(box: Rect2, from: Vector2, to: Vector2, upright: bool,
		at: float) -> bool:
```

النمط كان مثبَّتاً عند `func` ويقف عند أوّل `)`، فيرى السطر الأوّل وحده —
و`upright` و`at` غير موجودين بالنسبة إليه. **Godot سمّى هذا الوسيط بالذات
والفاحص لم يسمّه.**

وهذه ليست ثغرة في الميزة الجديدة وحدها: الفاحص كُتب أصلاً لاصطدام محلّيّ
بمحلّيّ، وكان يفوته ذلك أيضاً في كلّ توقيع ملفوف في المشروع.

فأُضيف `joined()` الذي يطوي السطور الملفوفة قبل القراءة. ونوعان من اللفّ لا
واحد: `\` في آخر السطر، **وسطر ينتهي وقوسٌ ما زال مفتوحاً** — وهذا الثاني لا
يحتاج علامة إطلاقاً، وهو الطريقة التي كُتب بها كلّ توقيع ملفوف في هذا
المشروع. والأقواس تُعدّ خارج علامات الاقتباس، فنصّ يحوي `(` وحيداً لا يبتلع
بقيّة الملفّ. وأرقام السطور تبقى أرقام السطور الأصلية.

بعدها يمسك الثلاثة كلّها: ١٠٢ و١١٩ و١٣٥.

### والفواحص صارت عشراً

وكلّ أسماء الأصناف الداخلية في المشروع (واحد وعشرون: `Bone`، `Clip`،
`Layer`، `Page`، `Pin`، `Shot`، `Size`، `Group`…) فُحصت: لا اصطدام آخر.
