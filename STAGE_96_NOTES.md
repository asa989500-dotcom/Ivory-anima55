# المرحلة ٩٦ — الغرفة، والمفتاح، وثلاثة أماكن لا رابع لها

## ١ · أيقونة Character 360 — لم تُستبدل، ولكنّك رأيت ما رأيت بحقّ

الملفّ `assets/icons/character360.png` لم يُمسّ. أيقونة الذكاء الاصطناعي دخلت
باسمها الخاصّ `ai_design.png`.

**لكن ما حدث فعلاً أسوأ من تبديل أيقونة.** صفّ Character 360 في البوصلة يظهر
فقط في مشاريع الرسوم المتحركة — هذا شرطٌ قديم، مكتوبٌ سببه فوق الدالّة: الدوران
يحتاج شريطاً زمنياً يُمرَّر عليه، وصفحةٌ ساكنة لا لحظات فيها. وأنا وضعتُ صفّ
الذكاء الاصطناعي **فوقه مباشرةً**. ففي مشروع رسمٍ عادي، اختفى ذاك وظهر هذا في
مكانه بالضبط، تحت علامةٍ أخرى.

يُقرأ ذلك كغرفةٍ أُخذت واستُبدلت. وهو شيءٌ مفزع أن يُرى، ولم يكن صحيحاً.

**أُصلح بأمرين:**

- صفّ الذكاء الاصطناعي انتقل إلى **آخر** البوصلة. لم يتحرّك شيء ممّا كان فيها.
- صفّ Character 360 صار **يظهر دائماً**، وفي مشروعٍ بلا شريط زمني يظهر خافتاً
  ومعه سطر: «افتحها في مشروع رسوم متحركة — الدوران يحتاج شريطاً زمنياً
  ليُمرَّر عليه.»

صفٌّ يختفي هو صفٌّ يظنّ أحدهم أنّه أُخذ. الآن الغرفة مرئيّة دائماً، وسبب
تعذّر دخولها مكتوبٌ تحتها بدل أن يُترك غيابٌ يُفسَّر.

**والغرفة نفسها لم يُمسّ منها شيء:** `room360.gd` ١١٩٤ سطراً، خمس وأربعون
دالّة، و`character360.gd` ٩٠٣ أسطر. كلّ ما بنيتَه فيها — النقاط والحلقات ووضع
Connect والاختبار والتصدير إلى الشريط الزمني — كما هو. الإضافة الوحيدة إليها
هي ملء الفراغات بالذكاء الاصطناعي، وهي أحد الأماكن الثلاثة المتّفق عليها.

## ٢ · الذكاء الاصطناعي أُزيل من قائمة الإعدادات

صفحة `الإعدادات ◂ الذكاء الاصطناعي` وصفّها في القائمة والمسار المؤدّي إليها —
حُذفت كلّها (٦٨ سطراً).

وكلّ رسالة كانت تقول «ضعه في الإعدادات ◂ الذكاء الاصطناعي» صارت تقول أين هو
فعلاً: سطر `API_TOKEN`. عشر رسائل عبر أربعة ملفّات — تعليمةٌ تشير إلى بابٍ
أُغلق أسوأ من ألّا تكون هناك تعليمة.

### والأماكن الثلاثة، مُحصاة لا مُدّعاة

مُرّر المشروع كلّه بحثاً عن كلّ مدخلٍ إلى الذكاء الاصطناعي:

| | |
|---|---|
| `tool_panels.gd:1126` | صفّ البوصلة |
| `layers_panel.gd:949` | زرّ «تحسين» في إعدادات الطبقة |
| `room360.gd:405` | ملء الفراغات، تلقائياً، في غرفة ٣٦٠ |

ثلاثة. لا رابع.

## ٣ · المفتاح

في `scripts/ai_assistant.gd` السطر **٣٣**. التطبيق يتّصل الآن.

**وشيئان يجب أن تعرفهما لا أن تكتشفهما:**

ثابتٌ مُجمَّع داخل APK يُستخرج منه بأمر `strings` على الملفّ. فهذا البناء
ممتاز لتحتفظ به وتجرّبه، وفي اللحظة التي تعطي فيها الـ APK لشخصٍ لا تعرفه،
يصير المفتاح مفتاحه، وHugging Face تحاسب حسابك أنت.

وقد مرّ المفتاح في محادثة. تدويره من `huggingface.co/settings/tokens` يستغرق
ثوانيَ ويُبطل القديم في كلّ مكان — افعل ذلك متى شككتَ في شيء، ولا تنتظر سبباً.

والباب الثاني ما زال مفتوحاً: مفتاحٌ في `user://ai_key.txt` يتقدّم على الثابت.
لا شيء في التطبيق يكتب ذلك الملفّ الآن، لكنّه يعني أنّ نسخةً لشخصٍ آخر يمكن أن
تُشحن بالثابت فارغاً والمفتاح يُوضع على الجهاز.

## ٤ · واختبارٌ صار يقول قاعدةً بدل جواب

`test_ai.gd` كان يفترض أنّ الثابت هو النصّ النائب، فوجود مفتاحٍ حقيقي كان
سيُسقطه. صار يتحقّق من **القاعدة**: الثابت يُحتسب مفتاحاً حين لا يكون النصّ
النائب — فيصحّ في الحالتين.

```
python3 tools/check_all.py .        # ٨ من ٨
godot --headless --path . --script tests/run_tests.gd
```
