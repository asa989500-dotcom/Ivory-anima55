# المرحلة ٩٤ — أنت محقّ، لم أكن دقيقاً

## ما ظهر لك

```
canvas_marks.gd:661  Identifier "size" not declared in the current scope
canvas_marks.gd:793  The parameter "p" is never used in the function "fill_mask()"
```

كلاهما من نفس الأداة، ونفس صنف العطل الذي أفلت مرّتين قبل هذه.

**الأول:** `size` هو عرض القماش وارتفاعه — خاصّية يرثها `CanvasView` من
`Control`. حين كان الكود داخل `canvas_view.gd` كان الاسم المجرّد صحيحاً.
وبعد رفعه إلى وحدةٍ مستقلّة لم يعد يعني شيئاً. صار `p.size`.

**الثاني:** `fill_mask` قرارُ شكلٍ على عرضٍ وارتفاع، ولا شيء من الرسم يدخل
فيه. الأداة أعطته قماشاً لا حاجة له به، فاضطرّ كلّ مُستدعٍ لحمله بلا سبب.
حُذف المعامل.

## لكن الحقيقة الأهمّ

**سبعة فواحص قرأت هذا الملفّ وقالت إنّه نظيف.**

- `check_identifiers` يتتبّع الأسماء التي تبدأ بشرطة سفلية فقط، و`size` لا تبدأ بها
- `check_references` يتتبّع `شيء.عضو` فقط، وهذا اسمٌ مجرّد
- ولا شيء كان ينظر إلى الأسماء المجرّدة إطلاقاً

والسبب أنّ السؤال في معظم المشروع بلا جواب: صنفٌ يرث `Control` يرث معه مئات
الأسماء من المحرّك، ولا فاحص يعمل بلا Godot يستطيع معرفتها.

**لكنّه سؤالٌ له جواب** لصنفٍ ينتهي نسبه عند `RefCounted` — وهو بالضبط ما تكون
عليه كلّ وحدةٍ مرفوعة من ملفٍّ كبير، وهي بالضبط الملفّات التي يقع فيها هذا
الخطأ، لأنّها تحمل كوداً كان يعيش في مكانٍ نطاقه أوسع.

## `tools/check_scope.py`

لأربعين صنفاً «عادياً» في المشروع: كلّ اسمٍ مجرّد يجب أن يكون متغيّراً محلّياً،
أو معاملاً، أو شيئاً يصرّح به الملفّ، أو صنفاً في المشروع، أو تحميلاً تلقائياً،
أو أحد عوالم اللغة نفسها. وما عدا ذلك يُبلَّغ عنه.

قائمة عوالم اللغة مكتوبةٌ باليد وناقصةٌ بالضرورة — وهذا هو الاتّجاه الآمن:
مدخلٌ ناقص يعطي إنذاراً كاذباً يُقرأ ويُصلَح في دقيقة، أمّا **فحصٌ** ناقص فيعطي
بناءً مكسوراً على تابلت أحدهم.

**وأُثبت** بإعادة العطل نفسه عمداً:

```
scripts/canvas_marks.gd:661  "size" is not declared here and is not a global
```

وفيه أيضاً فحصٌ ثانٍ للمعاملات غير المستعملة — نفس قاعدة Godot — **فوجد اثنين
آخرين لم تصل إليهما بعد**: `ease_lead` في `timeline_draw.gd`، و`look_words` في
`room360_ai.gd`. كلاهما أُعطي كائناً لا يستعمله. حُذف من كليهما.

## وثلاثة أخطاء في الأداة نفسها

`lift_module.py` كانت تعيد الكتابة داخل **التعليقات** أيضاً. النتيجة خمس عشرة
جملة عبر ثلاثة ملفّات، كلّها تُترجَم بلا خطأ ولا تعني شيئاً:

> «it becomes its own p.layers, which is the whole point»
> «throws away every p.view the model drew»

أُصلحت الجمل، وصارت الأداة تُنحّي التعليقات كما تُنحّي النصوص. وهذا ثالث عطلٍ
من هذه العائلة فيها: النصوص (٩١)، وأسماء المعاملات المتصادمة (٩٢)، والآن
التعليقات والمعاملات الزائدة.

## وشيءٌ آخر وجدته أثناء ذلك

`webp_anim.gd` كانت تستدعي `char()`. هذا عالَمٌ من Godot 3؛ وفي Godot 4 الطريق
هو `String.chr()`. وهو يُقرأ عند كلّ تصدير متحرّك بصيغة WebP. لم أراهن على مَن
منّا محقّ في اسمٍ — استعملت الصيغة التي لا لبس فيها.

## وكنسٌ للمشروع كلّه

بما أنّ الأدوات صارت موجودة، مُرّر المشروع كلّه بحثاً عن بقيّة ما يحذّر منه
Godot: متغيّرات محلّية غير مستعملة، وتعابير قائمة بذاتها. **صفر في الاثنين.**

## الحساب

| | ٩٠ | ٩١ | ٩٢ | ٩٣ | ٩٤ |
|---|---|---|---|---|---|
| فواحص | ٤ | ٦ | ٦ | ٧ | **٨** |

وكلّ واحدٍ من الأربعة الجديدة أُضيف بعد أن أفلت منه عطلٌ وصل إليك: المراجع بين
الملفّات، وأطوال الملفّات، وبنية المسافة البادئة، والآن النطاق والمعاملات.

**وما يبقى خارج الشبكة، بصراحة:** الملفّات التي ترث `Control` أو `Node` —
`main.gd` و`canvas_view.gd` و`timeline_panel.gd` و`room360.gd` — لا يمكن فحص
أسمائها المجرّدة بلا Godot. لكنّها ليست الملفّات التي يُرفَع إليها الكود، وهي
أقلّ عرضةً لهذا بكثير. أقول هذا حتى تعرف حدّ ما بنيتُه لا لتكتشفه بنفسك.

```
python3 tools/check_all.py .        # ٨ من ٨
godot --headless --path . --script tests/run_tests.gd
```
